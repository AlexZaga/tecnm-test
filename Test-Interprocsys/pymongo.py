import flask
from flask import *
import pymongo
from pymongo import MongoClient


'''

import pymongo
from pymongo import MongoClient

# Reemplaza la siguiente URL con la URL de tu base de datos MongoDB
mongo_url = "mongodb://localhost:27017/"

# Crea una instancia del cliente MongoClient
client = MongoClient(mongo_url)

# Accede a la base de datos (reemplaza 'mydatabase' con el nombre de tu base de datos)
db = client.mydatabase

# Accede a una colección dentro de la base de datos (reemplaza 'mycollection' con el nombre de tu colección)
collection = db.mycollection

'''
